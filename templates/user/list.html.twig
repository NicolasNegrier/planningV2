{% extends 'base.html.twig' %}

{% block title %}Les agents
{% endblock %}

{% block body %}

	<h1>Liste des agents enregistrés</h1>

	<a href="/admin/user/create" class="btn btn-primary">Ajouter un agent</a>

	<ul>
		{% for user in users %}
			<li>
				<a href="/user/{{ user.id }}">{{ user.name }}
					{{ user.firstname}}</a>
			</li>
		{% endfor %}
	</ul>
</div>{% endblock %}
