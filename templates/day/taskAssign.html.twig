{% extends 'base.html.twig' %}

{% block title %}Assignation agents/créneaux
{% endblock %}

{% block body %}
	<h1>Assignation d'agents aux créneaux pour le poste
		{{ dump(dailyTask) }}</h1>
	<div
		class="row">{# {{ form_start(formView) }}
											{{ form_errors(formView) }}
											{{ form_widget(formView) }}
											<button type="submit" class="btn btn-primary">
												Attribuer
											</button>
											{{ form_end(formView) }} #}
	</div>
{% endblock %}
